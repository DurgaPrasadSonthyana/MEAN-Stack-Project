import { CharNUmberOnlyDirective } from './char-number-only.directive';

describe('CharNUmberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharNUmberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
